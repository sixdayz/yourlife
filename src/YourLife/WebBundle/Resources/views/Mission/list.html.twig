{% extends 'YourLifeWebBundle:Root:index.html.twig' %}

{% block content %}
    {% if missions is not null %}
        <table class="table">
            <tr>
                <th>Заголовок</th>
                <th>Описание</th>
                <th>Очки за миссию</th>
                <th>Уровень миссии</th>
                <th>Секунд на выполнение</th>
                <th>Действия</th>
            </tr>
            {% for mission in missions %}
                <tr>
                    <td>{{ mission.title }}</td>
                    <td>{{ mission.description }}</td>
                    <td>{{ mission.points }}</td>
                    <td>{{ mission.userLevel }}</td>
                    <td>{{ mission.executionTime }}</td>
                    <td>
                        <a href="{{ path('your_life_web_mission_show', {id: mission.id}) }}">V</a>
                        <a href="{{ path('your_life_web_mission_remove', {id: mission.id}) }}">X</a>
                    </td>
                </tr>
            {% endfor %}
        </table>
    {% else %}
        <h2>Миссий нет</h2>
    {% endif %}
{% endblock %}